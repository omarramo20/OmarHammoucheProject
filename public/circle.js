class Cercle{

  constructor(x,y,rayon ) {
    this.pos = createVector(x,y);
    this.rayon = rayon; // pour commencer
  
    
  this.colorR = random(255);
  this.colorG = random(255);
  this.colorB = random(255);

}

}